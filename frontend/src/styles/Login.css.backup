/* ============================================
   LOGIN PAGE - Laboratory Console Aesthetic
   ============================================ */

.login-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-lg);
  position: relative;
  overflow: hidden;
  
  /* Deep atmospheric gradient */
  background: 
    radial-gradient(ellipse at 20% 80%, rgba(196, 115, 53, 0.08) 0%, transparent 50%),
    radial-gradient(ellipse at 80% 20%, rgba(91, 143, 168, 0.06) 0%, transparent 40%),
    linear-gradient(180deg, var(--color-base) 0%, var(--color-base-dark) 100%);
}

/* === MOLECULAR GRID BACKGROUND === */
.login-grid-overlay {
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: 0.4;
}

.molecular-grid {
  width: 100%;
  height: 100%;
  color: var(--color-accent-muted);
}

/* === MAIN LAYOUT === */
.login-main {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-xl);
  width: 100%;
  max-width: 480px;
  position: relative;
  z-index: 1;
}

@media (min-width: 1024px) {
  .login-main {
    max-width: 960px;
    grid-template-columns: 1fr 1.2fr;
    grid-template-rows: auto auto;
    gap: var(--space-2xl);
  }
  
  .login-brand {
    grid-column: 1;
    grid-row: 1 / 3;
    align-self: center;
  }
  
  .login-card {
    grid-column: 2;
    grid-row: 1;
  }
  
  .credentials-panel {
    grid-column: 2;
    grid-row: 2;
  }
}

/* === BRAND SECTION === */
.login-brand {
  text-align: center;
  padding: var(--space-xl);
}

@media (min-width: 1024px) {
  .login-brand {
    text-align: left;
    padding: var(--space-2xl);
  }
}

.brand-mark {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 72px;
  height: 72px;
  margin-bottom: var(--space-lg);
  background: var(--color-surface);
  border: 2px solid var(--color-accent);
  border-radius: var(--radius-lg);
  box-shadow: 
    var(--shadow-raised),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
}

.brand-icon {
  width: 40px;
  height: 40px;
  color: var(--color-accent);
}

.brand-title {
  font-family: var(--font-display);
  font-size: 3rem;
  font-weight: 700;
  letter-spacing: -0.03em;
  color: var(--color-text-primary);
  margin: 0 0 var(--space-xs) 0;
  line-height: 1;
}

.brand-subtitle {
  display: block;
  font-family: var(--font-ui);
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--color-accent);
  text-transform: uppercase;
  letter-spacing: 0.15em;
  margin-bottom: var(--space-lg);
}

.brand-tagline {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  font-family: var(--font-ui);
  font-size: 0.8125rem;
  color: var(--color-text-secondary);
  justify-content: center;
}

@media (min-width: 1024px) {
  .brand-tagline {
    justify-content: flex-start;
  }
}

.tagline-accent {
  color: var(--color-accent);
  font-size: 0.625rem;
}

/* === LOGIN CARD === */
.login-card {
  background: var(--color-surface);
  border: 1px solid rgba(244, 241, 235, 0.06);
  border-radius: var(--radius-lg);
  padding: var(--space-xl);
  box-shadow: 
    var(--shadow-floating),
    inset 0 1px 0 rgba(255, 255, 255, 0.03);
  
  /* Subtle top accent line */
  position: relative;
}

.login-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: var(--space-lg);
  right: var(--space-lg);
  height: 2px;
  background: linear-gradient(90deg, 
    transparent 0%, 
    var(--color-accent) 20%, 
    var(--color-accent) 80%, 
    transparent 100%
  );
  border-radius: 0 0 2px 2px;
}

.card-header {
  margin-bottom: var(--space-xl);
  text-align: center;
}

.card-header h2 {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--color-text-primary);
  margin: 0 0 var(--space-xs) 0;
}

.card-subtitle {
  font-family: var(--font-ui);
  font-size: 0.8125rem;
  color: var(--color-text-tertiary);
  margin: 0;
}

/* === FORM STYLES === */
.login-form {
  display: flex;
  flex-direction: column;
  gap: var(--space-lg);
}

.input-group {
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}

.input-label {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
}

.label-text {
  font-family: var(--font-ui);
  font-size: 0.75rem;
  font-weight: 600;
  color: var(--color-text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.label-indicator {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, 
    rgba(244, 241, 235, 0.1) 0%, 
    transparent 100%
  );
}

.input-field {
  font-family: var(--font-ui);
  font-size: 0.9375rem;
  padding: var(--space-md);
  background: var(--color-surface-sunken);
  border: 1px solid rgba(244, 241, 235, 0.08);
  border-radius: var(--radius-md);
  color: var(--color-text-primary);
  transition: all var(--transition-fast);
  
  /* Inset effect */
  box-shadow: var(--shadow-inset);
}

.input-field::placeholder {
  color: var(--color-text-tertiary);
}

.input-field:hover:not(:disabled) {
  border-color: rgba(244, 241, 235, 0.15);
}

.input-field:focus {
  outline: none;
  border-color: var(--color-accent);
  box-shadow: 
    var(--shadow-inset),
    0 0 0 3px rgba(196, 115, 53, 0.15);
}

.input-field:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* === ERROR ALERT === */
.error-alert {
  display: flex;
  align-items: flex-start;
  gap: var(--space-sm);
  padding: var(--space-md);
  background: rgba(196, 92, 77, 0.1);
  border: 1px solid rgba(196, 92, 77, 0.3);
  border-left: 3px solid var(--color-danger);
  border-radius: var(--radius-md);
  animation: fadeInUp 0.3s ease-out;
}

.error-icon {
  color: var(--color-danger);
  font-size: 1rem;
  line-height: 1;
}

.error-text {
  font-family: var(--font-ui);
  font-size: 0.8125rem;
  color: var(--color-text-primary);
}

/* === SUBMIT BUTTON === */
.submit-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-sm);
  padding: var(--space-md) var(--space-lg);
  margin-top: var(--space-sm);
  
  font-family: var(--font-ui);
  font-size: 0.875rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  
  color: var(--color-base-dark);
  background: linear-gradient(180deg, 
    var(--color-accent-bright) 0%, 
    var(--color-accent) 100%
  );
  border: none;
  border-radius: var(--radius-md);
  cursor: pointer;
  
  box-shadow: 
    0 2px 8px rgba(196, 115, 53, 0.3),
    inset 0 1px 0 rgba(255, 255, 255, 0.2);
  
  transition: all var(--transition-fast);
}

.submit-btn:hover:not(:disabled) {
  transform: translateY(-1px);
  box-shadow: 
    0 4px 16px rgba(196, 115, 53, 0.4),
    inset 0 1px 0 rgba(255, 255, 255, 0.2);
}

.submit-btn:active:not(:disabled) {
  transform: translateY(0);
}

.submit-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.submit-btn.is-loading {
  pointer-events: none;
}

.btn-arrow {
  transition: transform var(--transition-fast);
}

.submit-btn:hover .btn-arrow {
  transform: translateX(4px);
}

.loading-spinner {
  width: 16px;
  height: 16px;
  border: 2px solid rgba(10, 46, 54, 0.3);
  border-top-color: var(--color-base-dark);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* === CARD FOOTER === */
.card-footer {
  margin-top: var(--space-xl);
  padding-top: var(--space-lg);
  border-top: 1px solid rgba(244, 241, 235, 0.06);
  text-align: center;
}

.version-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-sm);
  font-family: var(--font-ui);
  font-size: 0.6875rem;
  color: var(--color-text-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.version-dot {
  width: 6px;
  height: 6px;
  background: var(--color-success);
  border-radius: 50%;
  animation: pulse 2s ease-in-out infinite;
}

/* === CREDENTIALS PANEL === */
.credentials-panel {
  background: var(--color-surface-sunken);
  border: 1px solid rgba(244, 241, 235, 0.04);
  border-radius: var(--radius-lg);
  padding: var(--space-lg);
}

.panel-header {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  margin-bottom: var(--space-md);
  padding-bottom: var(--space-md);
  border-bottom: 1px solid rgba(244, 241, 235, 0.06);
}

.panel-icon {
  color: var(--color-accent-muted);
  font-size: 0.75rem;
}

.panel-header h3 {
  font-family: var(--font-ui);
  font-size: 0.6875rem;
  font-weight: 600;
  color: var(--color-text-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin: 0;
}

.credentials-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}

.credential-row {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: var(--space-sm);
  align-items: center;
  padding: var(--space-sm);
  background: rgba(244, 241, 235, 0.02);
  border-radius: var(--radius-sm);
  transition: background var(--transition-fast);
}

.credential-row:hover {
  background: rgba(244, 241, 235, 0.04);
}

.role-badge {
  font-family: var(--font-ui);
  font-size: 0.625rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  padding: 2px 6px;
  border-radius: var(--radius-sm);
  white-space: nowrap;
}

.role-owner {
  background: rgba(196, 115, 53, 0.2);
  color: var(--color-accent);
}

.role-publisher {
  background: rgba(91, 143, 168, 0.2);
  color: var(--color-info);
}

.role-admin {
  background: rgba(74, 157, 124, 0.2);
  color: var(--color-success);
}

.role-faculty {
  background: rgba(212, 168, 75, 0.2);
  color: var(--color-warning);
}

.role-student {
  background: rgba(168, 181, 184, 0.15);
  color: var(--color-text-secondary);
}

.credential-value,
.credential-pass {
  font-family: var(--font-ui);
  font-size: 0.6875rem;
  color: var(--color-text-secondary);
  background: transparent;
  padding: 0;
  border: none;
}

.credential-pass {
  text-align: right;
  color: var(--color-text-tertiary);
}

/* === RESPONSIVE ADJUSTMENTS === */
@media (max-width: 480px) {
  .login-page {
    padding: var(--space-md);
  }
  
  .login-card {
    padding: var(--space-lg);
  }
  
  .brand-title {
    font-size: 2.25rem;
  }
  
  .credential-row {
    grid-template-columns: 1fr;
    gap: 2px;
  }
  
  .credential-pass {
    text-align: left;
  }
}
